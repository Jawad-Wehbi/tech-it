import { Container, Paper } from '@mui/material';
import * as React from 'react';
import { Colors } from '../../values/colors';


const OutputPanel = () => {
	return (
		<Container>
			<Paper sx={{ height: '52vh', padding: '10px', bgcolor: Colors.question_bg }}>

      </Paper>
		</Container>
	);
};

export default OutputPanel;
